25 lines per function
5 functions per file
no global variables
struct can only be in typedef in header file
no terner
no do while
no for
line cannot
Lines cannot exceed 80 characters
no leaks are tolerate (can use valgrind fir test)
you cannot modifie minilibx or libft files
you can use only:
• open, close, read, write,
printf, malloc, free, perror,
strerror, exit, gettimeofday.
• All functions of the math
library (-lm man man 3 math).
• All functions of the MinilibX
library.
for functions library.
functions take a maximum of 4 arguments